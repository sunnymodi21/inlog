(function () {
  'use strict';

  angular
    .module('samples')
    .controller('SamplesController', SamplesController);

  SamplesController.$inject = ['$scope', '$state', '$window'];

  function SamplesController($scope, $state, $window) {
    var vm = this;

    // Remove existing Sample
    
  }
}());
